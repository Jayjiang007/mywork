# Jay Jiang Portfolio

Static portfolio site for PPT / Keynote designer Jay Jiang.

## Preview

Open `index.html` directly in a browser, or serve this folder with any static server.

## Image hosting later

All portfolio image paths are centralized in `script.js`. After the visual direction is approved, replace the local paths with hosted image URLs from the selected image host.
